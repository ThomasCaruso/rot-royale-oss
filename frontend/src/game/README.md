# game/

Phaser 3 code: the hub scene and shared game utils (PLAN.md §10). Phaser scenes read the same
theme token palette as the DOM so the hub re-skins with the app. Empty in M0; the hub lands in M5.
(The v1 round-module mini-games — `pattern_sequence`, `odd_one_out` — are DOM/SVG, not Phaser.)
