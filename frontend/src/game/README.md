# game/

Phaser 3 code: the hub scene and shared game utils. Phaser scenes read the same theme token
palette as the DOM so the hub re-skins with the app. Scaffolding — no module imports Phaser yet.
(The v1 round-module mini-games — `pattern_sequence`, `odd_one_out` — are DOM/SVG, not Phaser.)
