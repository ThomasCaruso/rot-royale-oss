# Content banks

Seed question banks (JSON/CSV) for round modules, plus the loader that reads them into the
`questions` table. See `docs/architecture.md` §12 for the content boundary and the ingest rules.
